# Claude Code (user-specific settings and skills)
.claude/

# Dependencies
node_modules/

# Environment
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
*~
.idea/
.vscode/

# Build output
dist/
.vite/

# Deployment-supplied brand assets (#189). A deployment drops its own
# logo here and points `BRANDING.mark.logo` at it; the repo ships none,
# because the pilot company's mark is private (ADR-0001). Ignored so a
# private asset cannot be swept into a public repo by `git add -A`, and
# so the branding suites' fixtures leave no untracked residue.
public/brand/

# Test output
coverage/
test-results/
playwright-report/
.playwright-mcp/
# Demo master encode — large generated binary, staged here for Vimeo
# upload. The film lives on Vimeo; only the hero loop (assets/) is committed.
demo-clips/
# Playwright shared auth state — generated per-run, contains a live
# session cookie, must never be committed.
e2e/.auth/

# WIP scratch directory (local only)
docs/wip/

# Server-side runtime data (dev-generated VAPID key, etc.) — never committed.
# Repo-root only: unanchored, this also swallowed src/server/data/, hiding a
# tracked source file from every tool that honours .gitignore — Prettier since
# it is a default --ignore-path, so format:check silently skipped it.
/data/

# Scratch screenshots (Playwright MCP saves to repo root by default).
# Repo-root only — keeps any deliberate `assets/*.png` etc. unaffected.
/*.png
/*.jpg
/*.jpeg

# Pixabay stock photos used only by the opt-in demo recording flow
e2e/fixtures/demo/*.jpg
scripts/demo/assets/phone-backdrop.jpg
