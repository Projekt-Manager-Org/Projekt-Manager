# Module Map gating record (AC-350) — generated by
# `bash scripts/check-module-map.sh --update-gated`.
#
# The directories that carried a '#### <dir>' subsection under
# ARCHITECTURE.md's '### Directory Detail' when this was written.
# Dropping a subsection un-gates that directory; the check fails
# until the line here goes too, so opting out lands as a
# reviewable diff rather than a side effect of a doc edit.
src/server/
src/server/routes/
src/server/services/invoice/
